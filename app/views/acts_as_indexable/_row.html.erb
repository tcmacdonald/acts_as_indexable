  <tr id="<%= dom_id(row) %>" class="<%= local_assigns.try(:[], :klass) %>">
  <% decorated_attrs.each do |attr|  %>
    <%= content_tag :td, class: attr.try(:klass), colspan: attr.try(:colspan, row) do %>
      <%= attr.render(row) %>
    <% end if attr.is_visible?(row) %>
  <% end %>
  </tr>
